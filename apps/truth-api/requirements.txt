fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.5.2
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
email-validator==2.2.0
pytest==8.3.3
pytest-cov==5.0.0
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=17.0.0
tqdm>=4.66.0
lancedb>=0.13.0
openai>=1.0.0

# ---------------------------------------------------------------------------
# Phase 1 memory activation — NOT active by default.
# Uncomment and install only when MEMORY_LONGTERM_ENABLED=true and infra validated.
# Preferred: pip install -e '.[memory]'  (see pyproject.toml [memory] group)
# Alternative: pip install 'mem0ai>=0.1.25,<0.2'
# Note: httpx is already listed above and satisfies mem0ai's transport dep.
# Verify exact tag at PyPI before pinning: https://pypi.org/project/mem0ai/
# ---------------------------------------------------------------------------
# mem0ai>=0.1.25,<0.2
